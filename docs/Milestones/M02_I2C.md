# M02 – I2C Scanner

## Goal

Implement a simple I2C scanner.

The scanner reports only responding I2C addresses.

It does not identify module types and does not attempt to detect address conflicts.

## Planned API

```cpp
BB.i2c.scan(Serial);